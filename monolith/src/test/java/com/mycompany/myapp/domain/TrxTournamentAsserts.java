package com.mycompany.myapp.domain;

import static com.mycompany.myapp.domain.AssertUtils.bigDecimalCompareTo;
import static org.assertj.core.api.Assertions.assertThat;

public class TrxTournamentAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertTrxTournamentAllPropertiesEquals(TrxTournament expected, TrxTournament actual) {
        assertTrxTournamentAutoGeneratedPropertiesEquals(expected, actual);
        assertTrxTournamentAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertTrxTournamentAllUpdatablePropertiesEquals(TrxTournament expected, TrxTournament actual) {
        assertTrxTournamentUpdatableFieldsEquals(expected, actual);
        assertTrxTournamentUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertTrxTournamentAutoGeneratedPropertiesEquals(TrxTournament expected, TrxTournament actual) {
        assertThat(expected)
            .as("Verify TrxTournament auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertTrxTournamentUpdatableFieldsEquals(TrxTournament expected, TrxTournament actual) {
        assertThat(expected)
            .as("Verify TrxTournament relevant properties")
            .satisfies(e -> assertThat(e.getName()).as("check name").isEqualTo(actual.getName()))
            .satisfies(e -> assertThat(e.getType()).as("check type").isEqualTo(actual.getType()))
            .satisfies(
                e ->
                    assertThat(e.getPrizeAmount())
                        .as("check prizeAmount")
                        .usingComparator(bigDecimalCompareTo)
                        .isEqualTo(actual.getPrizeAmount())
            )
            .satisfies(e -> assertThat(e.getStartDate()).as("check startDate").isEqualTo(actual.getStartDate()))
            .satisfies(e -> assertThat(e.getEndDate()).as("check endDate").isEqualTo(actual.getEndDate()))
            .satisfies(e -> assertThat(e.getLocation()).as("check location").isEqualTo(actual.getLocation()))
            .satisfies(e -> assertThat(e.getMaxParticipants()).as("check maxParticipants").isEqualTo(actual.getMaxParticipants()))
            .satisfies(e -> assertThat(e.getStatus()).as("check status").isEqualTo(actual.getStatus()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertTrxTournamentUpdatableRelationshipsEquals(TrxTournament expected, TrxTournament actual) {
        assertThat(expected)
            .as("Verify TrxTournament relationships")
            .satisfies(e -> assertThat(e.getEvent()).as("check event").isEqualTo(actual.getEvent()));
    }
}
